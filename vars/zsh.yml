---

dircolors_config_file: "{{ shell_config_file | default(zsh_config_file) }}"

dircolors_configure_block: |
  autoload -Uz compinit && compinit

  eval "$( dircolors -b "{{ dircolors_dest_file }}" )"
  zstyle ':completion:*:default' list-colors ${(s.:.)LS_COLORS}

  alias ls='ls --color=auto'
